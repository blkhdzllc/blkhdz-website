name: Walmart Inventory Tracker

on:
  schedule:
    # Runs automatically every 15 minutes
    - cron: '*/15 * * * *' 
  workflow_dispatch: 
    # Allows you to trigger it manually from the Actions tab

jobs:
  check-inventory:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        # Reuses the requirements from your existing scraping setup
        run: pip install -r requirements.txt 

      - name: Run Walmart Scraper
        env:
          # Pulls your Scrape.do API token from the repository secrets
          SCRAPE_DO_TOKEN: ${{ secrets.SCRAPE_DO_TOKEN }} 
        run: python scripts/walmart_tracker.py
